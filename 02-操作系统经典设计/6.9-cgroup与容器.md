# 6.9 cgroup v2 与容器时代的资源控制

> Docker 和 Kubernetes 已经成为标准部署方式。容器需要精确的资源限制和隔离。

---

## 问题

- 多个容器共享内核，如何保证不互相影响？
- CPU、内存、IO、PID 数量如何精确限制？
- 内存压力下，如何避免容器被 OOM Killer 误杀？

---

## 经典解法：cgroup v2

**cgroup v2 统一了所有资源控制器**：

```
cgroup 层级结构：
/sys/fs/cgroup/
├── cpu.max                    # CPU 时间和周期
├── memory.min/memory.max      # 内存限制
├── io.max                     # 磁盘 IO 限制
├── pids.max                   # PID 数量限制
└── cgroup.controllers          # 可用控制器列表
```

---

## 容器内存限制实战

### Docker

```bash
docker run --memory=1g --memory-swap=1g \
           --cpus=2 --cpuset-cpus="0,1" \
           myapp
```

### Kubernetes Pod

```yaml
resources:
  limits:
    memory: "1Gi"
    cpu: "2"
  requests:
    memory: "512Mi"
    cpu: "1"
```

---

## Memory QoS（内存服务质量）

```bash
# 内存低水位标记（below this, 页面优先被回收）
echo 10240 > /sys/fs/cgroup/memory/myapp/memory.low

# 内存高水位标记（above this, 强制回收）
echo 20480 > /sys/fs/cgroup/memory/myapp/memory.high

# 保护量（保护重要进程不被 OOM Killer 杀掉）
echo 10240 > /sys/fs/cgroup/memory/myapp/memory.min

# 压力级别（触发 memory.reclaim）
echo 4096 > /sys/fs/cgroup/memory/myapp/memory.wmark_scale
```

### 内存水位示意图

```
memory.min    memory.low     [工作区]     memory.high    memory.max
    ↓              ↓             ↓            ↓              ↓
[受保护区]   [优先回收区]   [正常运行区]   [强制回收区]   [触发OOM]
```

---

## CPU 限制的精度控制

```bash
# 限制到 50% CPU（相对于一个核）
echo "50000 100000" > cpu.max

# 限制到 0.5 个 CPU
echo "50000 100000" > cpu.max

# 共享 CPU（不限制，但分配权重）
echo "1024 1024" > cpu.weight  # 默认 1024
echo "2048 1024" > cpu.weight  # 这个 cgroup 权重是默认的 2 倍
```

---

## cgroup v2 的改进（相比 v1）

| 特性 | cgroup v1 | cgroup v2 |
|------|-----------|-----------|
| 层级 | 多层级（每个控制器独立） | 单一层级（所有控制器统一） |
| 通知机制 | 通过事件fd | 统一的 notification |
| 线程模型 | 线程也可以是治理单位 | 只允许进程 |
| 内存奖励/惩罚 | 复杂 | 通过 memory.low/high 简洁表达 |

---

*相关章节：[6.2 内存管理] — 内存限制与 OOM Killer；[6.7 进程与线程] — cgroup 资源隔离；[6.3 同步互斥] — cgroup 中的并发控制*